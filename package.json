{
  "name": "bizzy-invoicing",
  "version": "2.0.0",
  "description": "Complete invoicing platform with password recovery - Get paid faster!",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd public/client && npm install && npm run build",
    "deploy": "npm run build && npm start",
    "heroku-postbuild": "cd public/client && npm install && npm run build"
  },
  "keywords": ["invoicing", "pwa", "stripe", "business", "entrepreneur", "saas"],
  "author": "BIZZY Team",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "stripe": "^14.0.0",
    "dotenv": "^16.3.1",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "body-parser": "^1.20.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
