# Example environment file for the project.
# Copy this file to ".env" and fill in real secret values. Do NOT commit your
# .env file or any real API keys to source control.

# Stripe keys (do not hardcode in source). See: https://stripe.com/docs/keys#safe-keys
# STRIPE_SECRET_KEY is used on the server (keep this private).
STRIPE_SECRET_KEY=

# STRIPE_PUBLISHABLE_KEY can be used in client code (public key).
STRIPE_PUBLISHABLE_KEY=

# Optional: webhook signing secret for verifying events from Stripe.
STRIPE_WEBHOOK_SECRET=

# Other environment variables (examples):
# DATABASE_URL=
# NEXT_PUBLIC_API_URL=
